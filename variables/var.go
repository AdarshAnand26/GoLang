package main

import "fmt"

func main(){
	// var name string="suru"
	// name:="suru"
	//Note: if we declare any veriable in go then we have to use it otherwise its give an error
	var name="suru"
	fmt.Println(name)
}
